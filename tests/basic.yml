version: "1.1"
name: Status Check
env:
  BASE_URL: https://example.com
config:
  http:
    baseURL: ${{ env.BASE_URL }}
tests:
  example:
    steps:
      - name: GET request
        http:
          url: /
          method: GET
          check:
            status: /^20/
