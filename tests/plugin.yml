version: "1.1"
name: "Hello Plugins"
tests:
  example:
    steps:
      - name: Plugin
        plugin:
          id: "../../tests/plugin.js"
          params:
            hello: world
          check:
            reply: world
